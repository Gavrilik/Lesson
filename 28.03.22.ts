//Array.from()
//Метод Array.from() создаёт новый экземпляр Array из массивоподобного или итерируемого объекта.
console.log(Array.from("фывфывфывфывфывфы"));

//String.fromCodePoint()
//Статический метод String.fromCodePoint() возвращает строку, созданную из указанной последовательности кодовых точек.
console.log(String.fromCharCode(6205854, 45, 6205854));

//Number.isNaN()
//Метод Number.isNaN() определяет, является ли переданное значение NaN

console.log(Number.isNaN(12));
console.log(Number.isNaN("12"));
console.log(Number.isNaN(NaN));
